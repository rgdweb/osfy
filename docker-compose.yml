services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tecos-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://tecos:Tc0s_S3cur3_2026!xK9m@host.docker.internal:5432/tecos?sslmode=disable
      - NODE_ENV=production
      - JWT_SECRET=tecos-secret-key-super-segura-2024
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
